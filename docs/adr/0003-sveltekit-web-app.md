# Use SvelteKit for the web app

The app will use SvelteKit with TypeScript for the mobile-first web experience. SvelteKit is a good fit for fast iteration on stateful planning UI and polished responsive interactions with less boilerplate than a React-heavy stack, while still supporting server-side data access and deployable full-stack routes; Next.js would offer a larger ecosystem but more framework weight than this project currently needs.
